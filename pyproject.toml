[tool.pytest.ini_options]
markers = [
    "slow: Marks tests as slow",
    "integration: Marks tests integration tests",
]
pythonpath = ["."]

[tool.ruff]
line-length = 88
indent-width = 4
target-version = "py311"

[tool.ruff.lint]
select = [
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "D",   # pydocstyle
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "SIM", # flake8-simplify
    "UP",  # pyupgrade
]
ignore = []

# Exclude a variety of commonly ignored directories.
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "venv",
]

[tool.ruff.lint.per-file-ignores]
"**/tests/**" = [
    "D100",  # Ignore warnings on test modules.
    "D102",  # Ignore warnings on test methods in test classes.
    "D103",  # Ignore warnings on top level test functions.
    "D105",  # Missing docstring in magic method
    "D107",  # Missing docstring in __init__

]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.isort]
combine-as-imports = true
force-wrap-aliases = true
known-first-party = ["lumiere"]
known-local-folder = ["lumiere.research.tests"]
lines-after-imports = 2
relative-imports-order = "closest-to-furthest"
section-order = ["future", "standard-library", "third-party", "first-party", "local-folder"]

[tool.coverage.run]
source = ["lumiere"]
omit = ["lumiere/research/tests/*", "*/migrations/*", "venv/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]
show_missing = true
skip_covered = false

[tool.coverage.html]
directory = "htmlcov"

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false

[[tool.mypy.overrides]]
module = ["torch.*", "wandb.*", "transformers.*", "pytest.*", "datasets.*"]
ignore_missing_imports = true

[tool.lumiere.discovery]
# Registry configuration for the discovery system
# Each entry configures how a specific type's registry should be created

[tool.lumiere.discovery.registries.tokenizer]
type = "lumiere.tokenizer.Tokenizer"
name = "tokenizer"
base_module = "lumiere.tokenizers"
discovery_paths = ["."]

[tool.lumiere.discovery.registries.dataset]
type = "lumiere.data.dataset.Dataset"
name = "dataset"
base_module = "lumiere.data.datasets"
discovery_paths = ["."]

[tool.lumiere.discovery.registries.pipeline]
type = "lumiere.data.pipeline.Pipeline"
name = "pipeline"
base_module = "lumiere.data.pipelines"
discovery_paths = ["."]

[tool.lumiere.discovery.registries.preprocessor]
type = "lumiere.data.preprocessor.Preprocessor"
name = "preprocessor"
base_module = "lumiere.data.preprocessors"
discovery_paths = ["."]

[tool.lumiere.discovery.registries.optimizer]
type = "torch.optim.Optimizer"
name = "optimizer"
base_module = "lumiere.training.optimizers"
discovery_paths = ["."]

[tool.lumiere.discovery.registries.scheduler]
type = "torch.optim.lr_scheduler.LRScheduler"
name = "scheduler"
base_module = "lumiere.training.lr_schedulers"
discovery_paths = ["."]

[tool.lumiere.discovery.registries.component]
type = "torch.nn.Module"
name = "component"
base_module = "lumiere.nn"
discovery_paths = ["attention", "feedforward", "embedding", "blocks"]
hierarchical = true
